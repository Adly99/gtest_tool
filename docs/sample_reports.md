# GTest Architect: Sample Reports (v3.0)

This document showcases the high-fidelity output generated by the **Autonomous v3.0 Engine**.

## 1. Markdown Coverage Gap Report
A human-readable summary of what's missing and how to test it.

# Coverage Gap Report: `TransactionProcessor.cpp`

*Source File: `src/core/TransactionProcessor.cpp`*  
*Generated: 2026-03-24T20:45:00Z by GTest Architect v3.0 (Autonomous Engine)*

## Summary Dashboard

✅ `[██████████████░░░░░░]` **70% covered**

| Metric | Count |
|--------|-------|
| Total Scenarios | **10** |
| ✅ Fully Covered | **7** |
| 🟠 Partially Covered | **1** |
| 🔴 Uncovered | **2** |
| **Coverage %** | **70%** |

## Detailed Gaps

### [🔴 UNCOVERED] BRANCH-004-InsufficentFunds 🔴 *(95% confidence)*
- **Type**: error_path
- **Expected Behavior**: Function should return `StatusCode::INSUFFICIENT_FUNDS` when balance < amount.
- **Requirement Source**: LLD Section 4.2.1 (Overdraft Protection)

> **📎 Context:** *"If the account balance after fee deduction is less than the transfer amount, the transaction must be aborted immediately."*

**💡 Assertion Hints:**
- Use `EXPECT_CALL(mockDB, GetBalance(_)).WillOnce(Return(50.0))`
- Verify `Process(100.0) == StatusCode::INSUFFICIENT_FUNDS`

---

## 2. JSON Gap Artifact
Machine-parseable data for CI/CD integration.

```json
{
  "metadata": {
    "analysisVersion": "3.0",
    "toolName": "GTest Architect",
    "generatedAt": "2026-03-24T20:45:00Z",
    "coveragePercent": 70.0
  },
  "gaps": [
    {
      "scenario_id": "BRANCH-004-InsufficentFunds",
      "status": "uncovered",
      "confidence_score": 0.95,
      "assertion_hints": [
        "Use EXPECT_CALL(mockDB, GetBalance(_)).WillOnce(Return(50.0))",
        "Verify Process(100.0) == StatusCode::INSUFFICIENT_FUNDS"
      ]
    }
  ]
}
```

---

## 3. Autonomous GTest File
One-call full file generation showing mocks and fixtures.

```cpp
#include <gtest/gtest.h>
#include <gmock/gmock.h>
#include "TransactionProcessor.h"
#include "MockDatabase.h"

using namespace testing;

class TransactionProcessorTest : public Test {
protected:
    MockDatabase mockDB;
    TransactionProcessor* processor;

    void SetUp() override {
        processor = new TransactionProcessor(&mockDB);
    }

    void TearDown() override {
        delete processor;
    }
};

/**
 * Scenario: BRANCH-004-InsufficentFunds
 * Proof: Verifies that overdraft logic correctly aborts transaction.
 */
TEST_F(TransactionProcessorTest, AbortsOnInsufficientFunds) {
    // Arrange
    EXPECT_CALL(mockDB, GetBalance(_)).WillOnce(Return(50.0));

    // Act
    auto result = processor->Process(100.0);

    // Assert
    EXPECT_EQ(result, StatusCode::INSUFFICIENT_FUNDS);
}
```
